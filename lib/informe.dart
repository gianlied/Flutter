import 'package:flutter/material.dart';


class Informe extends StatelessWidget {
  final Color color;
  Informe(this.color);
  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}
